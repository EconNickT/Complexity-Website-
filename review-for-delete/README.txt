Files quarantined as deletion candidates — all are 1-byte stubs with no content:

- _headers-emptied: the emptied _headers file (you removed its contents after it broke the banking-deserts map; the empty shell remained)
- banking-desert-map-stub: 1-byte file that sat in the repo root
- images-dots-stub: 1-byte file literally named "..." from images/
- banking-deserts-dots-stub: 1-byte file named "..." from projects/banking-deserts/

Nothing here is referenced anywhere. Delete the folder when ready.
